<Window x:Class="RESTful.AuthenticationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AuthenticationWindow" Height="300" Width="310">
    <!-- Parent Grid -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <!-- Grid to split cell -->
        <Grid x:Name="authGrid" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Margin="3" Content="Field"></Label>
            <Label Grid.Row="0" Grid.Column="1" Margin="3" Content="Value"></Label>
        </Grid>
        <Button Grid.Row="1" Grid.Column="0" Margin ="3" Width="150" Content="Build Authentication" Click="BuildAuthentication_Click"></Button>
    </Grid>
</Window>
